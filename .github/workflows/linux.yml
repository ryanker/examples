name: CI (Linux)

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - master

jobs:
  build_and_test:
    name: ${{ matrix.version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        version: ["stable", "nightly"]

    steps:
      - uses: actions/checkout@v2

      - name: cache cargo registry
        uses: actions/cache@v1
        with:
          path: ~/.cargo/registry
          key: cargo-registry-linux-${{ matrix.version }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-registry-linux-${{ matrix.version }}-

      - name: cache cargo index
        uses: actions/cache@v1
        with:
          path: ~/.cargo/git
          key: cargo-index-linux-${{ matrix.version }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-index-linux-${{ matrix.version }}-

      - name: cache cargo target
        uses: actions/cache@v1
        with:
          path: ./target/debug
          key: cargo-registry-linux-${{ matrix.version }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-registry-linux-${{ matrix.version }}-

      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ matrix.version }}
          override: true

      - name: cargo check
        uses: actions-rs/cargo@v1
        timeout-minutes: 40
        with:
          command: check
          args: --workspace --bins --examples --tests

      - name: start redis
        uses: supercharge/redis-github-action@1.1.0
        with:
          redis-version: 6

      - name: cargo test
        uses: actions-rs/cargo@v1
        timeout-minutes: 40
        env:
          # included in example of redis, not clear its purpose
          CI: true
        with:
          command: test
          args: --workspace --all-features --no-fail-fast -- --nocapture
